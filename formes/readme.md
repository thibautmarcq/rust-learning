## formes

A small Rust example that models geometric shapes through a `Forme` trait.

The program implements area and perimeter calculations for circles, rectangles, and squares. It also demonstrates generic functions, trait objects with `Box<dyn Forme>`, finding the shape with the largest area, and calculating a total area.

This is an exercise in traits, polymorphism, generics, and collections of heterogeneous values.
