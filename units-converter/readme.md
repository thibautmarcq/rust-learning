## units-converter

A small Rust command-line program for converting temperatures.

The program lets the user choose between Celsius-to-Fahrenheit and Fahrenheit-to-Celsius conversion, then reads the temperature and prints the result. Conversion functions are kept in the separate `conversion` module.

This is an exercise in Cargo project structure, modules, standard input, numeric parsing, and ownership basics.
