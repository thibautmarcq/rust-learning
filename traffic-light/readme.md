## traffic-light

A small Rust example that models a traffic light as a state machine.

The program cycles through red, orange, and green states in response to user input. It also demonstrates a failure state carrying a reason with an enum variant and uses pattern matching to determine the next state.

This is an exercise in enums, pattern matching, state transitions, and owned data.
