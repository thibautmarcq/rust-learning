## text-analyzer

A small Rust command-line program that analyzes a text file.

The program reads a file path from standard input and reports the number of lines, total words, longest word, and three most frequent words. Word frequencies are counted with a `HashMap` and sorted by frequency.

This is an exercise in file I/O, string slices, iterators, collections, and basic error handling.
